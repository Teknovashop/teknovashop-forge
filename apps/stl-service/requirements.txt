fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic>=2.5
# Supabase SDK v2 (necesitamos FileOptions)
supabase==2.4.0

# Si usas generación real de STL, añade tus libs (trimesh, numpy, etc.)
# numpy==2.0.1
# trimesh==4.4.9
